[user]
name = Dustin Krysak
username = bashfulrobot
email = dustin@bashfulrobot.com
signingkey = 64EA7EA1E11512164668E22913A72DEF6C9670B7

[core]
# editor = code-insiders
whitespace = fix,-indent-with-non-tab,trailing-space
excludesfile = ~/.gitignore

[web]
browser = brave-browser

[color]
ui = auto

[github]
user = bashfulrobot

[alias]
unpack-refs = "!bash -c 'IFS=$''\\n''; for f in $(git show-ref --heads); do /bin/echo ''Writing  '' $(echo $f | cut -c42-); echo $(echo $f | cut -c1-40) > \"${GIT_DIR:-.git}/$(echo $f | cut -c42-)\"; done'"
